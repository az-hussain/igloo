# Machine-specific configs (generated by setup)
.claude/settings.json
.claude/settings.local.json
.claude/allowed-senders.json
.claude/session-id
.claude/tools.json
.claude/scheduler-state.json
core/TOOLS.md
mcp/mcp-config.json

# Daemon runtime
daemon/*.log
daemon/*.log.old
daemon/*.pid

# Node
mcp/node_modules/
scripts/node_modules/
daemon/node_modules/

# OS
.DS_Store

# Memory files are tracked (they're the agent's brain)
# Don't ignore memory/ or tasks/
